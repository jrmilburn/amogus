# Completed work

## 2026-09-05 — Foundation

### #1 — Implemented; local acceptance verified

- Created pnpm workspace: shared constants/types, Node 22 + Colyseus 0.17 server, Vite + PixiJS v8 client. Dependencies locked in `pnpm-lock.yaml`.
- Added placeholder room (10-player capacity), `/health` with live room count, port validation, graceful shutdown, and shared 20 Hz tick constant.
- Client renders an original spinning beacon, connects through same-origin `/ws`, displays/logs its session ID, and handles connection failure/retry. Responsive layout and reduced-motion support included.
- Added dev/build commands, strict TypeScript, ESLint/Prettier, GitHub Actions, architecture/commands in `CLAUDE.md`, and setup instructions in `README.md`.
- Verified lint, typecheck, build, and integration tests. Tests cover shared-room connections, full-room rejection, overflow room creation, health counts (including locked rooms), and disposal.
- Rechecked tests/typecheck under Node 22.23.2; isolated production bundle starts, serves health, and shuts down cleanly on SIGTERM.
- Chrome verification at 1440×900 and 360×740: canvas rendered, real `/ws` connection established, no page errors or horizontal overflow; simulated connection failure and retry passed. No physical-device FPS measurement yet.
- GitHub CI workflow is written but has not run remotely; no PR or commit created.

### #2 — Implementation ready; hosting acceptance pending

- Added multi-stage non-root `Dockerfile.server`, Caddy static hosting and `/ws` proxy (prefix stripped for HTTP matchmaking and WebSockets), and server/Caddy/cloudflared Compose services.
- Added `.env.example`, executable `scripts/host.sh`, and laptop hosting/troubleshooting instructions. Public URL and tunnel token must be supplied in `.env`.
- Verified Compose configuration, shell syntax, the pinned cloudflared image manifest, and the production bundle generated by `pnpm deploy --prod`.
- Docker daemon is stopped. Container build/start/restart, Caddy traffic, HTTPS tunnel connection, and phone-on-mobile-data acceptance remain unverified. No tunnel credentials were available or configured.

## 2026-09-05 — Shared protocol and lobby

### #3 — Implemented and locally verified

- Added shared Player/GameState/Settings/MeetingState/SabotageState schemas, all planned message payload types and names, map IDs, 12 named/numbered colours, settings defaults/ranges, and movement sequence acknowledgement.
- Resolved plan conflicts: shared now depends on `@colyseus/schema`; roles stay in private server records/messages, outside public Player state. Contracts and units documented in `packages/shared/PROTOCOL.md`.

### #4 — Implemented and locally verified

- Added unique 5-letter rooms, create/join/invite flow, sanitized profiles, exclusive colours, live readiness, all host settings, capacity enforcement, host migration, and guarded start.
- Start deliberately stops at `starting` until #9; explicit notice and temporary host return-to-lobby action included. Readiness is advisory; two impostors require seven players.
- `/impeccable` applied to landing, lobby, profiles, and settings. User requirement to apply it to **every screen, including basic UI**, is persisted in `AGENTS.md` and `CLAUDE.md`.
- Verification passed: lint, typecheck (including test sources), all three tests, and production build. Tests/build were run under Node 22. Server integration covers state sync, private room listings, invalid/unauthorized messages, full/in-progress joins, host leave/drop migration, and exclusion of roles from wire state.
- Four-browser Chrome check passed: create/join/invite, profiles/colours, readiness/reset, host settings, start/return, host transfer, clipboard fallback, and invalid/missing-room errors. Desktop 1440px and mobile 360px screenshots reviewed; no page errors or horizontal overflow. Settings collapse initially on phones.
- Reviewed protanopia/deuteranopia/tritanopia simulations. Some hues converge, so stable numbers and names remain essential. Core text contrast pairs exceed 7.7:1. Impeccable's mechanical detector ran in degraded regex mode; this was not a full automated accessibility audit.
- Cross-machine/physical-phone lobby acceptance and tunnel hosting remain pending. No remote CI run, PR, or commit created. Existing development servers were already running at ports 5173/2567 and were left in place.

## 2026-09-05 — The Hollow map

### #5 — Implemented and locally verified

- Added canonical `packages/shared/maps/the-hollow.json`: 10 rooms (6 ring, 2 hubs, 2 dead ends), 10 spawns, 18 task stations covering all 8 minigames, 9 reciprocal vents, 20 doors, 6 sabotage panels, and 2 cameras.
- Added Zod `MapDefSchema`, shared geometry helpers, and separate `@mutiny/shared/maps` / JSON exports. Explicit corridor footprints and long-task stage links extend the proposed format; details are in `packages/shared/maps/README.md`.
- Generated 32 collision rectangles from the exact floor complement. `pnpm map:walls` regenerates them; `pnpm map:render` validates and produces `docs/maps/the-hollow.png` plus SVG.
- Outer circuit: 40,800 px / 160 px/s = 255 seconds (4m 15s), analytically measured with doors open. Collision clearance assumes a 24px player radius; #7 must preserve or revalidate it.
- Verification passed: lint, typecheck (including scripts and tests), all 10 tests (7 map + 3 existing), and production build under Node 22. Map checks cover containment, reciprocal vents, long-task chains, wall consistency, circuit clearance, fixture/spawn reachability, closed dead-end doors, and invalid edits. PNG/SVG regeneration and visual inspection passed.
- PNG is a technical diagram, not final art. No frontend screen changed in this step; #6 must use `/impeccable` for the renderer and any preview/loading UI.

## 2026-09-05 — Map renderer

### #6 — Implemented and locally verified; target-device FPS acceptance pending

- Added PixiJS Renderer with window resize/DPR support, fixed 1920×1080 world view and letterboxing, bounded camera follow, ordered floor/walls/objects/entities/lighting/HUD, y-sorted entities, static culling, and lifecycle cleanup.
- Applied `/impeccable` to the map art and preview/loading/error UI. Original cold-metal/amber atlas has 10 textures; deterministic `scripts/build-assets.ts` runs before client dev/build. Design and #7 integration notes: `docs/design/renderer.md`.
- Added lazy Explore buttons and `?view=map`, room selection, drag/touch/arrow camera movement, Back/Escape, progress/retry, and portrait guidance. Exploring does not leave a lobby or send gameplay actions.
- Chrome checks passed at desktop/landscape-phone/portrait sizes and DPR 1/2: rendering, culling, resize, entity sorting, failed/delayed loading, close/reopen, touch dragging, all ten room jumps, and lobby preservation. Local stationary frame intervals: ~16.7ms median / ~16.7–16.8ms p95; update CPU p95 ~0.1–0.2ms. This is not a physical-phone benchmark. Screenshots: `docs/maps/renderer-commons.png`, `docs/maps/renderer-mobile.png`.
- Verification passed: lint, typecheck, all 13 tests, and production build (tests/build under Node 22). Build emits two upstream Zod/Rollup comment-annotation warnings but succeeds. Required 2020-era laptop / mid-range Android FPS acceptance remains unverified. Ambient glow is present; occluded vision belongs to #13, movement to #7, and character art to #8.

## Resume here

1. Continue with #7 authoritative movement, prediction/reconciliation, interpolation, and desktop/touch input. #7–#24 are not implemented. Use the renderer's live camera target and entity APIs; preserve or revalidate the map's 24px collision radius.
2. #8 character art can follow #6. Continue using `/impeccable` for every screen, including basic HUD/loading UI.
3. For #9 assignment, choose only task records with `stage: 1`; follow `nextTaskId` for long-task destinations. There are three complete long assignments.
4. #2 Docker/tunnel/mobile-data acceptance, #4 physical two-device lobby acceptance, and #6 target-device FPS checks remain pending.
5. Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` after changes. Use Node 22 (`.nvmrc`); the machine defaults to Node 26.

`plan.md` remains the original specification. Update this file as each issue is implemented; distinguish code completion from external acceptance checks.
