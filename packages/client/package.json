{
  "name": "@mutiny/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm assets && vite --host 0.0.0.0",
    "build": "pnpm assets && tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "assets": "node --import tsx ../../scripts/build-assets.ts && node --import tsx ../../scripts/build-characters.ts",
    "test": "pnpm assets && node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@colyseus/sdk": "~0.17.0",
    "@mutiny/shared": "workspace:*",
    "pixi.js": "^8.14.0"
  },
  "devDependencies": {
    "happy-dom": "^20.14.0",
    "vite": "^7.2.0"
  }
}
