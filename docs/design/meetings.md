# Meetings — #16 and #17

Scope: reporting, emergency calls, discussion, living chat, voting and ejection, extending the incumbent station HUD. Mode: Experience with a focused Operate interruption. Full win conditions and ghost interaction remain #18.

F / Report appears within 100px of a visible body, with a coral body outline. C / Emergency appears within 80px of the Commons button; its remaining allowance and a nearby explanation show cooldown, exhausted allowance or crisis denial. Both use 64px touch targets and the existing coral/amber station palette. Pending requests disable conflicting actions and time out after five seconds.

Meeting entry shows a native dialog with a large reason, caller portrait rendered from the original three-layer engineer atlas, caller name/colour/number, and reported body colour/location. No role information is used. The caller snapshot survives departure. Focus enters the heading; Back/Escape returns to the room screen without resuming gameplay. Host migration updates the reset action. A portrait-rendering failure retains a text identity fallback. State-driven reopening avoids relying solely on a one-shot event.

## Verification

#17 extends the intro with a two-column roster/chat layout (stacked below 650px), persistent named/numbered player buttons, explicit dead crosses and Voted badges. Discussion gates ballot input; selecting a player or Skip requires a separate confirmation. Vote targets are never visible before tally. Chat uses a labelled 200-character input, bounded scrollable transcript, server acknowledgement, timeout recovery and draft preservation. Dead players have read-only access.

The six-second result sequence replaces roster/chat with original engineer art drifting across a responsive starfield. Role confirmation appears after 1.8s when enabled, remaining count after 3.5s, and server expiry resumes play. Reopening uses current public result/time rather than replaying an old event; reduced motion keeps the portrait static. Non-anonymous tallies show numbered voter stamps with accessible names. Public result, not client inference, supplies role confirmation. Host reset remains available as an escape hatch during development.

- #17 verification: server tests cover tie/skip/deadline behavior, immutable ballots, dead/disconnected/stale guards, all anonymity/confirmation combinations, no body after ejection, chat cap/throttle/filter/history and return-to-play timing. Six real clients exchange chat, lock a tied anonymous vote, return to play, then eject an impostor with non-anonymous ballots and role confirmation. Integration accelerates server deadlines; unit tests cover the full six-second boundary.
- Browser inventory remains empty. Rendered roster/chat layout, virtual keyboard, keyboard selection/confirmation, stable focus while other players vote, transcript scrolling, reduced-motion ejection and Back/reopen timing need browser/device acceptance. No gameplay screenshot or physical-device performance claim is made.
- #17 Impeccable detector returned no findings. Independent source review prompted once-only live announcements for the timed role/remaining-count reveals, including reopening partway through the sequence; role text still comes only from permitted server output.

- Server unit tests cover strict/forged/stale requests, life/vent/session guards, distance, walls, single transition, body clearing and teleport without resurrection, caller departure, per-player allowance, zero allowance, start/post-meeting cooldown hook and exact sabotage deadline precedence.
- A four-client integration reports a real kill, cancels another player's active task and a repair, clears vent records and the reactor fault, verifies post-patch meeting delivery and frozen movement, then resets and calls an emergency in round two. Existing sabotage/task/action regressions also run.
- The browser inventory is empty. Live screenshots, portrait extraction, touch controls, keyboard/focus, phone/short-landscape layout and transition rendering remain unverified. Check 1440×900, 360×740 and short landscape with a body near Commons, an active task/repair/vent, host departure, reset and Back/reopen. Ensure no stale bodies, movement, phase HUD or repair dialog remains over the intro.
- One Impeccable detector pass returned no findings. Independent source review prompted change-only updates to the emergency live-region text, avoiding repeated announcements every render frame. Three-row action layout on short devices remains a specific browser acceptance check.
